// difinisco un array con all'interno degli elementi

const myArray = ["pippo", "pluto", "paperino"];

// definisco due variabili per cercare i valori dentro l'array
const elem = "pluto";
const elem2 = 3;

// richiamo la funzione
console.log(isFindElem(myArray, elem));
console.log(isFindElem(myArray, elem2));

